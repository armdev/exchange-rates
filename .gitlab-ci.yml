image: lwieske/java-8

services:
  - docker:dind

build:
  stage: build
  services:
    - mysql:latest
  script:
    - ls -al
    - apt-get update
    - apt-cache search maven
    - apt-get install maven
    - mvn -version
  variables:
     MYSQL_DATABASE: root
     MYSQL_ROOT_PASSWORD: root