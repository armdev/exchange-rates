image: bandeszko/gitlab-runner-maven-java8-sonar

services:
  - docker:dind

build:
  stage: build
  services:
    - mysql:latest
  script:
    - ls -al /
    - maven --version
    - uname -r
  variables:
     MYSQL_DATABASE: root
     MYSQL_ROOT_PASSWORD: root