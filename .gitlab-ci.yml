#image: lwieske/java-8
image: bandeszko/gitlab-runner-maven-java8-sonar

services:
  - docker:dind

build:
  stage: build
  services:
    - mysql:latest
  script:
    - ls -al /
    - which maven
    - which java
    - uname -r
    #- "yum -y install maven"
    #- "mvn -version"
  variables:
     MYSQL_DATABASE: root
     MYSQL_ROOT_PASSWORD: root